#include "types.h"
#include "stat.h"
#include "user.h"
#include "fcntl.h"

int
main(int argc, char *argv[])
{
    if (argc < 2)
        exit();
    printf(1, "input is %s\n", argv[1]);

//    /* at zero */
//    arg = (char*) 651263;   // dummy();
//    if(write(fd, arg, 10) == -1)
//        printf(1, "caught\n");

    exit();
}
